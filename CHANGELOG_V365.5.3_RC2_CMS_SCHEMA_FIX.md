# Production V365.5.3 RC2 CMS Schema Fix

- 修復 Decap CMS Entry 編輯頁 React #130。
- 圖片欄位改為 `/assets/...` 路徑文字，媒體仍可透過「媒體」分頁上傳。
- Hero 輪播順序由 `slides` 清單拖曳排序控制。
- 後台寫入分支：`cloudflare-test`。
- 暫停 Editorial Workflow，改用 simple publish 進行 RC2 QA。
